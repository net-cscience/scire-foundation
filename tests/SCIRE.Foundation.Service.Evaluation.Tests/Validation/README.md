# Validation

.NET SDK 10.0.401, Release build: 39 tests passed; two opt-in live tests skipped.

No compiler warnings were emitted by the final Release build. NuGet packaging succeeded and the resulting archive contains the net10.0 assembly, XML documentation, README and declared dependencies.

The private DRES server was not contacted. Run the supplied live tests from its network.
